# Used at Docker build time on the Droplet (see piza-api docker-compose.prod.yml).
# Local production test:
#   docker build --build-arg NEXT_PUBLIC_API_URL=https://api.marinapizzas.com.au/api -t piza-front .

NEXT_PUBLIC_API_URL=https://api.marinapizzas.com.au/api
